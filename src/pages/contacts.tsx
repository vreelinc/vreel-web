import Contacts from "@edit/Contacts/Contacts/Contacts";
import React from "react";

type Props = {};

const contacts = (props: Props) => {
  return (
    <div>
      <Contacts />
    </div>
  );
};

export default contacts;
